import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sakina Cards',
  description: 'Deeper questions for deeper connections. Thoughtful conversation cards for Muslim couples, families, and friends.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400;1,600;1,700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
